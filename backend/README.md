# LLM Evaluation-as-a-Service Backend

FastAPI backend for rubric-based LLM evaluation with experiment tracking.

## Run

```
cp .env.example .env
pip install -r requirements.txt
uvicorn app.main:app --reload
```
